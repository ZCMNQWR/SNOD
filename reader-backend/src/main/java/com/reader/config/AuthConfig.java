package com.reader.config;

import org.springframework.context.annotation.Configuration;

/**
 * Placeholder configuration for authentication (Google OAuth2)
 *
 * TODO: When ready, enable Spring Security and wire an OAuth2 client registration
 * using the properties defined in `application.properties` (client-id / client-secret).
 */
@Configuration
public class AuthConfig {
    // Intentionally left minimal — add beans and security configuration here
}
